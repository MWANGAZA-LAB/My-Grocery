[variables]
NODE_ENV = "production"
CI = "false" 
GENERATE_SOURCEMAP = "false"
NODE_OPTIONS = "--max-old-space-size=512"

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmd = "npm run install"

[phases.build] 
cmd = "npm run build"

[start]
cmd = "npm start"
